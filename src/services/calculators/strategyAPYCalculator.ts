import { StrategyCoinAllocation, StrategyCoinAPY } from 'models'

export const strategyAPYCalculator = (
  allocation: StrategyCoinAllocation,
): StrategyCoinAPY => {
  throw Error(`Not implemented ${allocation}`)
}
