import { StrategyCoinAllocation } from 'models'

export const requestAllocationValidator = (
  request: StrategyCoinAllocation[],
): void => {
  throw Error(`requestAllocationValidator not implemented ${request}`)
}
